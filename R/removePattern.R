#' Delete objects in environment that match the pattern
#'
#' @details Quick function to cleanup environment. Remove all objects that match pattern.
#' @param my_pattern a string pattern that matches all the objects I want to remove from environment.
#' @examples
#' \dontrun{
#' removePattern("some_pattern")
#'}
#'
#' @export


# removePattern <- function(my_pattern){
#   rm(list = ls(pattern = my_pattern))
# }

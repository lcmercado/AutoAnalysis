#Place holder for function that tests the class type per column to make sure it is using the lightest possible field type:
#E.g. if only integers in column, use int, do not use long, double, etc.
#E.g. if only short strings use character
#E.g.g if repetitive strings, use factor
#etc.

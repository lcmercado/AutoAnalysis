#Function that takes strings or datasets as an argument
#then goes to available APIs to find its source and the API arguments to obtain it
#then tries to find the availability according to different fields: range, categories, etc.
#
#ARGUMENTS: input, list or group of APIs,
